# Skill Without Frontmatter

This skill file is missing the required frontmatter section.

It should cause a parsing error when the parser attempts to load it.

## Expected Behavior

The parser should:
1. Detect missing frontmatter
2. Return a descriptive error message
3. Not crash or throw uncaught exceptions

## Test Scenario

Use this file to verify error handling for missing frontmatter.
